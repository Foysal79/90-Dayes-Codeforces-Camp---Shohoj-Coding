#include <bits/stdc++.h>
using namespace std;
int main() 
{
    int t; cin >> t;

    while(t--) {
        long long int a, b, def, c = 0;
        cin >> a >> b;
        cout << ( abs(a-b) + 9 ) / 10 << endl; 
    }
    return 0;
}