#include <bits/stdc++.h>
using namespace std;
int main() 
{
    int t; cin >> t; 

    while(t--) {
        string s; cin >> s;
        int x = (s[0] - '0') + (s[1] - '0');
        cout << x << endl;
    }
    return 0;
}